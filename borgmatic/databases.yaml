source_directories: []
repositories:
  - path: ssh://<ID>@<ID>.repo.borgbase.com/./repo
    label: example.com - databases

mysql_databases:
  - name: nextcloud

<<: !include /etc/borgmatic.d/common/common.yaml
