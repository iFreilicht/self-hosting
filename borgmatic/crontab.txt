*/15 * * * * PATH=$PATH:/usr/local/bin /usr/local/bin/borgmatic create prune compact --stats -v 0 2>&1
# Once a month, run checks on all repositories
20 22 13 */1 * PATH=$PATH:/usr/local/bin /usr/local/bin/borgmatic check -v 0 2>&1